[package]
name = "arkit"
description = "Dioxus-based ArkUI application framework for OpenHarmony"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true

[features]
default = []
full = ["animation", "barcode", "camera-scan", "chart", "code", "i18n", "icon", "lottie-network", "markdown", "router", "shadcn", "terminal"]
animation = ["dep:arkit_animation"]
# Standalone barcode / QR generation (no camera required).
barcode = ["dep:arkit_barcode"]
camera = ["dep:arkit_camera"]
camera-scan = ["animation", "camera", "arkit_camera/scan"]
chart = ["animation", "dep:arkit_chart"]
# Standalone Code component + tree-sitter highlighting (no markdown required).
code = ["shadcn", "arkit_shadcn/code"]
i18n = ["dep:arkit_i18n"]
icon = ["dep:arkit_icon"]
lottie = ["dep:arkit_lottie"]
lottie-network = ["lottie", "arkit_lottie/network"]
lottie-expressions = ["lottie", "arkit_lottie/expressions"]
markdown = ["shadcn", "arkit_shadcn/markdown"]
# Convenience: Markdown fences use the Code pipeline when both are enabled.
markdown-highlight = ["markdown", "code"]
router = ["animation", "dep:arkit_router"]
shadcn = ["animation", "icon", "dep:arkit_shadcn"]
# Embedded terminal (libghostty-vt via Zig).
terminal = ["dep:arkit_terminal"]

[dependencies]
arkit_derive.workspace = true
arkit_runtime.workspace = true
arkit_arkui.workspace = true
arkit_prelude.workspace = true
arkit_hooks.workspace = true
arkit_i18n = { workspace = true, optional = true }
arkit_router = { workspace = true, optional = true }
arkit_animation = { workspace = true, optional = true }
arkit_barcode = { workspace = true, optional = true }
arkit_camera = { workspace = true, optional = true }
arkit_chart = { workspace = true, optional = true }
arkit_shadcn = { workspace = true, optional = true }
arkit_icon = { workspace = true, optional = true }
arkit_lottie = { workspace = true, optional = true }
arkit_terminal = { workspace = true, optional = true }
ohos-arkui-binding.workspace = true
openharmony-ability.workspace = true
napi-ohos.workspace = true
napi-derive-ohos.workspace = true
