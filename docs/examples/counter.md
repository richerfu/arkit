# counter

路径：`examples/counter`

最小 Dioxus/ArkUI 示例：`use_signal` 保存计数，`onclick` 修改 signal，动态 text mutation 由 HostTree 投影为 ArkUI `TextContent` 更新。

```sh
cd examples/counter
ohrs build --arch aarch
```
