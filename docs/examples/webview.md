# webview

路径：`examples/webview`

展示 `EmbeddedWebViewController`、`use_layout_frame_node`、URL/标题/缩放 signals，以及 native callback 通过 UI loop 更新 Dioxus 状态。WebView 支持由 `arkit_runtime` 直接提供，不需要 facade feature。
