# webview

路径：`examples/webview`

展示 `EmbeddedWebViewController`、`use_layout_frame_node`、URL/标题/缩放 signals，以及 native callback 通过 UI loop 更新 Dioxus 状态。WebView 是 `arkit` 默认 facade 能力，不需要额外 feature，也不需要应用直接依赖内部 runtime crate。
